version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12

  pre_build:
    commands:
      - npm install --force

  
  build:
    commands:
      - npm run build-dev
  
artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build